# Navan Connector — UI Component Plan

## Sidebar UI (`navan_sidebar`)
- Conforms strictly to `UI_INTERFACE_STANDARD.md` and `DUI` parameter contract.
- Root vertical stack with `align="stretch"` and `gap=3`.
- Clear header and product summary.
- Form inputs with explicit label texts and contextual placeholders.
- Dedicated help modal trigger explaining credential setup.
- App Settings action button in the footer.
